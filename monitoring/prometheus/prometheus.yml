# =============================================================================
# prometheus.yml — À COMPLÉTER (TP3 étape 4, TP4 étape 1)
# =============================================================================
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

# TODO J4 : alerting → alertmanager:9093

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # TODO J3 : job node-local (node-exporter:9100, labels env=lab)
  # TODO J3 : job taskflow-api (targets <ip web1>:80 env=staging, <ip web2>:80 env=prod, metrics_path /metrics)
  # TODO J3 : job node-vms (<ip>:9100, labels env)
  # TODO J4 : job cadvisor (cadvisor:8080)
  # TODO J4 : job blackbox-http (metrics_path /probe, params module=[http_2xx],
  #           relabel __address__ → __param_target, instance, __address__=blackbox-exporter:9115)
