# Captures d'écran — rendu évaluation

À déposer ici (ou envoyer au formateur) :

1. `runner-idle.png` — Settings → Actions → Runners : `lab-runner` en état **Idle** (labels `self-hosted`, `lab`)
2. `env-production.png` — Settings → Environments → production (Partie 2)
3. `deploy-prod-approved.png` — Actions → Deploy : job `deploy-prod` approuvé et vert (Partie 2)
